// tslint:disable:no-magic-numbers

import { Count, to } from '@musical-patterns/utilities'

const SECOND: Count = to.Count(2)
const THIRD: Count = to.Count(3)
const FOURTH: Count = to.Count(4)
const EIGHTH: Count = to.Count(8)

const XENHARMONIC_SERIES_SCALE_ITERATIONS: Count = to.Count(3)

export {
    SECOND,
    THIRD,
    FOURTH,
    EIGHTH,
    XENHARMONIC_SERIES_SCALE_ITERATIONS,
}
